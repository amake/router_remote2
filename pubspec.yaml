name: router_remote2
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.0
  shared_preferences: ^0.5.0
  flutter_bloc: ^4.0.0
  equatable: ^1.0.0
  connectivity: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
